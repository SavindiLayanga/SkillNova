{
  "name": "skillnova",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev:all": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run server\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
